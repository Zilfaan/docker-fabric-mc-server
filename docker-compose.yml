services:
  fabric:
    build: .
    container_name: fabric-server
    ports:
      - "25565:25565"
      - "25575:25575"
    volumes:
      - ./data:/data
      - ./.env:/data/.env
    restart: unless-stopped
